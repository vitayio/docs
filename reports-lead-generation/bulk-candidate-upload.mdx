---
title: "Bulk candidate upload"
description: "Add up to 50 candidates at once by uploading a formatted CSV file."
---

Instead of adding candidates one at a time, you can upload a CSV file to create many candidates
in a single step. You can optionally attach a reference request to each candidate at the same
time, so requests go out the moment the upload finishes.

<Note>
  You need a recruiter (or higher) role to upload candidates. Each upload reads the first **50
  rows** of the file — **100** if Vitay has enabled the raised limit for your account (ask
  **hello@vitay.io**). Rows past the limit are ignored silently, so split larger files rather
  than relying on a warning. Every row that fails validation rolls back the entire batch — no
  partial imports.
</Note>

## Prepare your CSV file

Download the official template first so your columns are in the right order.

<Steps>
  <Step title="Download the template">
    In the **Candidates** section, click **Import candidates**, then click **Download the CSV
    template**. Open the file in your spreadsheet app.

    ![SCREENSHOT: the import panel with the "Download the CSV template" link — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Fill in the required columns">
    Every row must have at minimum:

    | Column | Required | Notes |
    |--------|----------|-------|
    | `first_name` | Yes | |
    | `last_name` | Yes | |
    | `email` | Yes | Must be a valid email; cannot match an existing recruiter |
    | `internal_id` | No | Your ATS/CRM ID; auto-generated if blank |
    | `country_code` | No | Phone country code digits only (e.g. `1` for USA); defaults to `1` |
    | `phone_number` | No | Formatting (spaces, dashes, `+`) is stripped automatically |
    | `public_notes` | No | Message shown to the candidate on their reference request |
    | `internal_notes` | No | Notes visible to recruiters only |
    | `min_manager_references` | No | Minimum references required |
    | `is_manager_only` | No | `true` or `yes` to require all references to be managers |
    | `assigned_recruiter_email` | No | Assigns the candidate to a specific recruiter on your firm (or on the branch named by `branch_id`). Leave it blank and **you** become the assigned recruiter |
    | `branch_id` | No | Sub-account ID; assigns the candidate to a branch. Find it in the **ID** column of **Configuration → Accounts** |
    | `job_title` | No | Visible when the Job title feature is enabled on your account |

    The question set for auto-created reference requests is not set per row — you choose it once
    in the import panel and it applies to every candidate in the file.

  </Step>
  <Step title="Save as CSV">
    Save the file with UTF-8 encoding and a `.csv` extension. Remove any blank rows at the end.
  </Step>
</Steps>

## Upload the file

<Steps>
  <Step title="Open the import panel">
    In the **Candidates** section, click **Import candidates**.
  </Step>
  <Step title="Choose a question set (optional)">
    If you want Vitay to automatically send a reference request to every uploaded candidate,
    select a **question set** in the import panel — it applies to all rows in the file. Leave this
    blank to add candidates without sending requests.

    ![SCREENSHOT: the import panel with a question set selector and file-upload area — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Select your file">
    Click **Choose file** (or drag and drop) and pick your CSV. Vitay validates the file before
    committing anything.
  </Step>
  <Step title="Review the validation results">
    Vitay scans the file and shows:
    - **New entries** — rows that will be created.
    - **Errors** — rows that fail validation (missing required fields, invalid email format,
      recruiter email not found, a candidate that already exists, etc.). Duplicates — rows where
      the email or internal ID already exists on your firm — count as errors, not warnings.

    Fix any errors in your spreadsheet, re-export as CSV, and re-upload. Every error, duplicates
    included, must be resolved before you can proceed.

    ![SCREENSHOT: the validation summary screen showing new entries, duplicates, and errors — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Confirm the upload">
    When validation passes with no errors, click **Submit** to commit all rows. If a duplicate is
    discovered during the upload itself (for example, a candidate created between the check and
    the upload), the entire batch rolls back and nothing is imported. If you selected a question
    set, Vitay sends reference requests to every new candidate immediately after the import
    succeeds.
  </Step>
</Steps>

## What happens after a successful upload

- Each new candidate is created on your firm (or the branch specified by `branch_id`).
- The candidate is assigned to the recruiter in `assigned_recruiter_email`. If you left that
  column blank, the candidate is assigned to **you** — the person running the upload — and any
  reference request created for the row is sent in your name.
- If you chose a question set, a reference request is created and emailed to each candidate
  right away.

## Troubleshooting

<AccordionGroup>
  <Accordion title="A row failed because the recruiter email wasn't found">
    The email in `assigned_recruiter_email` must match a recruiter on your firm (or the branch
    specified by `branch_id`). Correct the email, or remove the column value and let the
    candidate be assigned to you.
  </Accordion>
  <Accordion title="Where do I find the Branch ID?">
    Open the **Configuration** area and go to **Accounts**. The first column, **ID**, is the
    number to put in `branch_id` — it is a short number, not the long UUID from the browser
    address bar. Leave the column blank to create the candidate on your own firm.
  </Accordion>
  <Accordion title="The upload says a candidate already exists but I don't see them">
    Duplicate detection includes inactive (archived) candidates. If a candidate with the same
    email or internal ID was previously archived, they still block a new upload. Restore or
    remove the existing record before re-uploading that row.
  </Accordion>
  <Accordion title="My file has more than 50 rows and won't import">
    Vitay reads the first 50 rows of the file — 100 for accounts with the raised limit, which
    Vitay enables on request (write to **hello@vitay.io**). Rows beyond the limit are simply not
    read, and nothing tells you they were skipped, so split your file and upload in batches.
  </Accordion>
  <Accordion title="I see a phone number error even though the number looks correct">
    Formatting characters — spaces, dashes, parentheses, and a leading `+` — are removed
    automatically, so a phone error means the number itself isn't valid. Check the digits and
    make sure `country_code` is set to the correct dialing prefix (e.g. `1` for North America,
    `44` for the UK).
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Add a candidate" icon="user-plus" href="/candidates-reference-requests/add-a-candidate" />
  <Card title="Send a reference request" icon="paper-plane" href="/candidates-reference-requests/send-a-reference-request" />
  <Card title="Download reports" icon="download" href="/reports-lead-generation/download-reports" />
  <Card title="Question sets and templates" icon="list-check" href="/account-configuration/question-sets-and-templates" />
</CardGroup>

{/* source: app.vitay.io bulk-upload UI; api app/controllers/api/v3/documents/csv_controller.rb (check and upload actions, candidates branch); api app/services/documents/csv/candidates_handler.rb (template fields: internal_id, first_name, last_name, email, country_code, phone_number, public_notes, internal_notes, min_manager_references, assigned_recruiter_email, branch_id, question_set_id, is_manager_only; optional job_title); api app/controllers/api/v3/documents/csv_controller.rb#check/#upload (content_raw.first(100) when setting.break_rows_limit_feature_flag else first(50); added_by = current_recruiter unless assigned_recruiter_email resolves; branch_id matched on hiring_firm.load_self_and_siblings.seq_id); api app/models/setting.rb feature flag :break_rows_limit 'Increase the rows of CSV from 50 to 100'; app.vitay.io projects/configuration protected/accounts/list (TABLES.HEADERS.ID -> seq_id column); api app/models/candidate.rb */}
