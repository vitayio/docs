---
title: "Plugins and the marketplace"
description: "What Vitay plugins are, how to see which ones are active on your account and request new ones, and which screens each plugin unlocks."
---

Plugins are optional features that the Vitay team switches on per account —
pre-screening, background screening, feedback, signatures, multiple languages
and white label. The **Plugins** page in the Configuration area (the
marketplace) shows which are active and lets you request the rest.

<Note>
  You need a **root recruiter** role to open the Configuration area, where the
  Plugins page lives. Plugins are activated by Vitay, not self-served: the
  switches on the page are read-only.
</Note>

## See your plugins and request one

<Steps>
  <Step title="Open Configuration → Plugins">
    Open the **Configuration** area from the main navigation and select
    **Plugins** (alongside Settings, Users and Integrations). The page is
    titled **Plugins** — *Enhance your experience and add exclusive features*.
  </Step>
  <Step title="Read the two sections">
    **Activated** lists the plugins on your account, each with a switch that is
    on and locked. **Deactivated** lists the rest.

    ![SCREENSHOT: the Configuration → Plugins page with the Activated and Deactivated sections — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Request a plugin">
    On a card under **Deactivated**, click **Request plugin**. This opens a
    pre-filled email to **hello@vitay.io** with the subject *Vitay.io -
    Request … plugin*. The Vitay team activates it for you; there is no
    self-service switch. To turn an active plugin off, email hello@vitay.io as
    well.
  </Step>
</Steps>

## The plugin cards

The marketplace shows these eight plugins:

| Card | What it adds | Read more |
|---|---|---|
| **Multi-languages** | Use Vitay in multiple languages: pick which languages are offered to candidates and references, with per-language templates and question sets. | [Settings → Plugins tab](#the-settings-plugins-tab) below, and [Email, text and page templates](/account-configuration/email-text-and-page-templates) |
| **Candidate Feedback** | Get feedback from candidates about your recruiting process. | [Candidate feedback](/feedback/candidate-feedback) |
| **Recruiter Feedback** | Ask candidates to rate the recruiters on your team. | [Candidate feedback](/feedback/candidate-feedback) |
| **Pre-screening** | Send pre-screening questionnaires to candidates before you check references. | [Pre-screening questionnaires](/candidates-reference-requests/pre-screening) |
| **Candidate signature** | Ask the candidate to sign at the bottom of the reference request form. | [Signatures](/account-configuration/signatures) |
| **Reference signature** | Ask the reference provider to sign at the bottom of the reference form. | [Signatures](/account-configuration/signatures) |
| **Background screening** | Order candidate background checks through Mintz Global Screening. | [Background screening requests](/candidates-reference-requests/background-screening) |
| **White label** | A white-label Vitay: custom domain, links, branding and emails. | [Branding and white label](/account-configuration/branding-white-label) |

Candidate signature and Reference signature are on by default for every
account; the others are off until requested.

### Vitay-managed plugins without a card

Two plugins are set up by Vitay and don't appear on the marketplace:

- **Validity Screening** — adds Validity Screening Solutions (United States)
  as a background screening partner. See
  [Background screening requests](/candidates-reference-requests/background-screening).
- **Data retention** — enables the **Data Retention** settings tab and the
  **Scheduled deletion** page for automatic anonymisation of old records. See
  [Data retention](/security-it/data-retention).

Email **hello@vitay.io** to ask about either.

## The Settings → Plugins tab

Some plugins have settings of their own. They live under **Configuration →
Settings → Plugins**:

- With **Multi-languages** off, the tab shows **Plugins not selected** — *You
  do not have any plugins that require configuration* — with a **Go to
  marketplace** link.
- With **Multi-languages** on, the tab shows the **Multi languages** card
  (*Set available languages in the language selecter*) with a checkbox per
  language: **English**, **Español**, **Français** and **Français (Canada)**.
  English is always on and can't be unticked. Ticking a language saves
  immediately and provisions that language's email, text and page templates.

![SCREENSHOT: Settings → Plugins with the Multi languages card and the four language checkboxes — placeholder](/images/placeholder.png)

## What each plugin unlocks

Plugin-gated screens are simply hidden until the plugin is active.

| Plugin | Screens and options it reveals |
|---|---|
| **Pre-screening** | **Pre-screening** in the main navigation; **Pre-screening** tab on candidate pages; **Pre-screening** tab in Quick request and in the bulk **Send new request** modal; the **Type** field on question sets (with a **Pre-screening** option) and a **Pre-screening** filter on the Question sets list; **Pre-screening** tab on the Overview dashboard; the Pre screening templates under Settings → Emails / Texts / Pages; the Pre-screening notification card. |
| **Candidate Feedback** / **Recruiter Feedback** | **Feedback** in the main navigation; **Feedback** tab on candidate pages; the **Type** field on question sets (with a **Feedback** option); feedback templates and notification cards; Recruiter Feedback also adds a **Feedback** tab on each user's page under Configuration → Users. |
| **Background screening** / **Validity Screening** | **Background screening** tab on candidate pages, with the partner's tab in the request modal. |
| **Candidate signature** / **Reference signature** | The signature card on the public reference request form / reference form; signature sections in side panels and on PDF reports. |
| **Multi-languages** | The Settings → Plugins language card; language pickers on the Emails, Texts, Pages and Leads settings tabs and on question sets. |
| **White label** | The **White Label** tab under Settings (greyed out otherwise). |
| **Data retention** | The **Data Retention** settings tab and the **Scheduled deletion** item in the Configuration navigation. |

<Note>
  A greyed-out navigation item with the tooltip **Feature locked** is not a
  missing plugin — it's what demo accounts see on items such as **Question
  sets** until the account is upgraded. Missing plugins never show a locked
  item; their screens are hidden entirely.
</Note>

## Troubleshooting

<AccordionGroup>
  <Accordion title="The switches on the Plugins page don't move">
    That's expected: plugin switches are locked and only show the current
    state. Use **Request plugin** (or email hello@vitay.io) to change it.
  </Accordion>
  <Accordion title="I requested a plugin but nothing changed">
    Activation is done by the Vitay team, so allow for a reply to your email.
    Once it's on, log out and back in so your session picks up the new
    settings.
  </Accordion>
  <Accordion title="I can't find the Plugins page">
    Only root recruiters have the Configuration area. Ask your account's root
    recruiter to check the page or to upgrade your role.
  </Accordion>
  <Accordion title="A plugin is Activated but I don't see its screens">
    Refresh the app or log out and back in — the navigation and candidate tabs
    are built from your settings at login.
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Manage users" icon="users" href="/account-configuration/manage-users" />
  <Card title="Firm configuration" icon="gear" href="/account-configuration/firm-configuration" />
  <Card title="Email, text and page templates" icon="envelope" href="/account-configuration/email-text-and-page-templates" />
  <Card title="Branding and white label" icon="palette" href="/account-configuration/branding-white-label" />
</CardGroup>

{/* source: app.vitay.io projects/configuration protected/plugins (plugins.component.ts card list — 8 rendered, others commented out; PLUGINS.ACTIVATED / DEACTIVATED / CTA "Request plugin" mailto hello@vitay.io; locked ui-switch), protected/settings/plugins (SETTINGS.PLUGINS.NOT_ACTIVATED.*, MULTI_LANGUAGES.*, languages helper en/es/fr/fr-ca with index 0 disabled), protected/settings/settings.component.ts (TABS.WHITE_LABEL isDisabled, TABS.DATA_RETENTION gated by plugins_json.data_retention_plugin), shared navbar (NAVBAR.SCHEDULED_ENTITIES gate), protected/users/show/feedbacks (is_plugins_recruiter_feedback_enabled); projects/references shared navbar (NAVBAR.LINKS.PRE_SCREENING / FEEDBACKS, NAVBAR.FEATURE_LOCKED shown when disabled = is_demo_enabled), protected/candidates/show setTabs (TABS.FEEDBACK / PRE_SCREENING / BACKGROUND_CHECKING gates), shared modals/question-sets (Type options), protected/question-sets/list filters, protected/dashboard filters; api app/models/setting.rb (is_plugins_* columns and defaults; PLUGINS = validity_screening, data_retention in plugins_json), app/services/settings/provision_language_templates.rb */}
