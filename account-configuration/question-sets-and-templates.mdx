---
title: "Question sets and templates"
description: "Create and manage the question sets that references answer, and use templates to speed up setup."
---

A **question set** is the list of questions Vitay sends to each reference when a reference request is sent. You can have multiple question sets — for example, one for executive roles and one for entry-level roles — and select the right one when you create a reference request.

<Note>
  You need a **root recruiter** or **supervisor recruiter** role to create, edit, or delete question sets. Regular recruiters can select from existing question sets but cannot modify them.
</Note>

## Create a question set

<Steps>
  <Step title="Open Question sets">
    Click **Question sets** in the left-hand sidebar of the main app.

    ![The Question sets list page](/images/question-sets.png)
  </Step>
  <Step title="Click the add button">
    Click the **plus** icon in the toolbar (its tooltip reads **Add question set**). The **Add question set** modal opens.
  </Step>
  <Step title="Give it a label">
    Enter a clear, descriptive name in the **Label** field so recruiters can identify the set in the dropdown when sending a reference request (for example, "Standard Reference Check" or "Healthcare — Clinical Roles").

    To start from one of Vitay's pre-built sets instead, see [Use question set templates](#use-question-set-templates) below.
  </Step>
  <Step title="Choose a language">
    Select the **language** references will answer in. If your plan includes
    multiple languages, a set can hold translations of its questions in several
    languages — use the language dropdown in the editor to view and edit each
    translation (switching the dropdown never changes the set itself). When a
    recruiter sends a request in a given language, only sets whose questions are
    fully translated into that language are offered.
  </Step>
  <Step title="Add questions">
    On the set's page, click the **plus** icon (tooltip **Add question**) and choose a question type in the **Add question** modal:

    | Type | When to use |
    |---|---|
    | **Text** | Open-ended written answer |
    | **Rating** | Numeric score on a scale (e.g., 1–5) |
    | **Multiple choice (Options)** | Pick one from a list |
    | **Radio** | Single-select from options displayed as radio buttons |
    | **Checkbox** | Multi-select from a list |
    | **Matrix** | Rate several items on the same scale in a grid |
    | **Slider** | Drag to a value on a continuous scale |
    | **Ranking** | Drag items into a ranked order |
    | **Date** | Pick a single date |
    | **Date range** | Pick a start and end date |

    For each question, enter the question text and — for choice-based types — add the answer options. Every question type except **Text** can also be marked **Commentable**, which adds an extra comment field for the reference (with an optional **Comment required** setting).
  </Step>
  <Step title="Organise questions into groups (optional)">
    If you have created **question groups** (labels that organise related questions), assign each question to a group in the **Question group** field. The group's label appears as a small heading above each of its questions in the reference form.

    ![SCREENSHOT: A completed question set with several questions and group labels — placeholder](/images/placeholder.png)
  </Step>
</Steps>

## Edit a question set

<Steps>
  <Step title="Find the question set">
    Open **Question sets** in the left-hand sidebar and click the set you want to change.
  </Step>
  <Step title="Make your edits">
    Add questions with the **plus** icon, edit a question by clicking it, and drag questions to change their order. Use the **pencil** icon to rename the set. Each change is saved when you submit its modal — there is no page-level Save. Changes only affect future reference requests — submitted answers are not altered.
  </Step>
</Steps>

## Set a default question set

You can mark one question set as the **default** so it is pre-selected when a recruiter creates a reference request.

On the **Question sets** list, click the **star** icon on the set's row (its tooltip reads **Highlight**). Clicking it again removes the default mark.

Only one set can be the default at a time: starring a set removes the mark from whichever set held it before. Duplicating a set never copies the default mark, so a copy of your default set starts out as a regular set.

## Duplicate or delete a question set

To reuse a set as a starting point, click the **duplicate** (copy) icon — on the set's row in the list, or in the toolbar on the set's page — and confirm. Vitay creates a copy you can edit freely.

To remove a set, open it and click the red **trash** icon, then confirm in the **Delete question set?** dialog. References already collected with that set are preserved, but the set no longer appears when recruiters send new requests, and it cannot be restored from the app — if you deleted a set by mistake, contact **hello@vitay.io**.

## Use question set templates

Vitay provides pre-built question set templates you can use as a starting point. They are used from inside the **Add question set** modal:

<Steps>
  <Step title="Open the Add question set modal">
    On the **Question sets** list, click the **plus** icon (tooltip **Add question set**).
  </Step>
  <Step title="Tick the template checkbox">
    Tick **Use a question set template**. A **Template** dropdown appears listing the available templates.
  </Step>
  <Step title="Pick a template">
    Select a template and submit. Vitay creates a new question set pre-populated with the template's questions — a copy you can then edit however you need; the template itself is never changed.

    ![SCREENSHOT: The Add question set modal with the Use a question set template checkbox ticked and the Template dropdown open — placeholder](/images/placeholder.png)
  </Step>
</Steps>

<Note>
  Question set templates are managed by Vitay. If you need a template added or modified, contact **hello@vitay.io**.
</Note>

## Question groups

**Question groups** are labels that let you organise related questions (for example, "Leadership" or "Technical skills"). A group's label is shown as a small heading above each of its questions in the reference form, which makes longer question sets easier to follow.

To manage groups, click the **Quest. groups** button at the top of the **Question sets** list. You can add, rename, or remove groups there — each group has a single **Label** field. Groups are shared across all question sets on your account.

## Troubleshooting

<AccordionGroup>
  <Accordion title="I can't select a question set when sending a reference request">
    There may be no question sets on your account, or they may all have been deleted. Open **Question sets** in the left-hand sidebar and check that at least one set exists. If the list is empty, create a new set following the steps above.
  </Accordion>
  <Accordion title="A question set doesn't appear for one of my languages">
    When sending a request, a set is only offered for a language once **every**
    question in it has a translation in that language. Open the set, switch the
    editor's language dropdown to the missing language, and fill in the
    untranslated questions.
  </Accordion>
  <Accordion title="I want different question sets for different relationship types">
    You can assign a question set to a specific reference relationship (e.g., "Direct manager" always gets the Manager question set). Go to **Settings → Relationships** and select a default question set for each relationship type there.
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Send a reference request" icon="paper-plane" href="/candidates-reference-requests/send-a-reference-request" />
  <Card title="Manage users" icon="users" href="/account-configuration/manage-users" />
  <Card title="Notification settings" icon="bell" href="/account-configuration/notification-settings" />
  <Card title="Read a completed reference" icon="clipboard-check" href="/candidates-reference-requests/read-a-completed-reference" />
</CardGroup>

{/* source: app.vitay.io projects/references NAVBAR.LINKS.QUESTION_SETS, question-sets.list.component (Quest. groups button, plus/Add question set, star Highlight = is_default, Duplicate); api app/models/question_set.rb (after_save clears sibling is_default — one default per firm), app/models/question_set_base.rb (deep_copy! does not copy is_default), question-sets.show.component (Add question, pencil, trash + Delete question set? confirm, drag-to-reorder), src/app/shared/components/modals/question-sets (Label field, Use a question set template checkbox + Template dropdown), src/app/shared/components/modals/questions (type/sentence/options/columns/Commentable except Text/Comment required/Question group), references.component.html group label above questions; api app/models/question_set.rb (pseudo_destroy), app/models/question.rb (type enum: O/T/R/D/C/M/S/K/A/E), app/models/question_group.rb, app/models/question_set_template.rb */}
