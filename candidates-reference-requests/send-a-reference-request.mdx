---
title: "Send a reference request"
description: "Build and send a reference request so a candidate can submit their references."
---

A reference request asks your candidate to provide their references. Once you
send it, the candidate receives an email (and a text message, if they have a
phone number on file) inviting them to enter each reference's contact details,
and Vitay sends those references the questions to answer.

<Note>
  You need a recruiter (or higher) role to send reference requests, and the
  candidate must already exist in Vitay with an email address or phone number.
  See [Add a candidate](/candidates-reference-requests/add-a-candidate).
</Note>

## Send a request

<Steps>
  <Step title="Open the candidate">
    Go to **Candidates** and select the candidate you want references for.
  </Step>
  <Step title="Start a reference request">
    Click **Reference request** to open the reference request form. The button
    only appears when the candidate has an email address or phone number on
    file.

    ![SCREENSHOT: the candidate page with the "Reference request" button — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Choose the question set">
    Select the **question set** the references will answer. This is the list of
    questions Vitay sends to each reference. Your firm's default question set
    is pre-selected for you.
  </Step>
  <Step title="Set how many references are required">
    Choose the **Number of references** the candidate must provide. The
    candidate can't finish until they've supplied at least this many. Tick
    **Managers only** if every reference must be a manager.

    The dropdown starts at your firm's default and offers the next four values
    — so with a firm default of 2 you can pick 2, 3, 4, 5 or 6. If this
    candidate has had a reference request before, the dropdown offers 1 to 5
    instead. A root recruiter sets the firm default on the **Ref. Requests**
    settings tab; see
    [Reference request settings](/account-configuration/reference-request-settings).
  </Step>
  <Step title="Add the optional details">
    You can always add notes in **Public notes for the candidate** — the
    candidate will see these. Depending on your firm's settings you may also be
    able to:
    - pick the **language** the request is sent in,
    - set a **scheduled date** to send the request later. You can only pick
      today or a future date, and the request goes out when a daily job picks
      up everything scheduled for that day — so a scheduled request is sent on
      the chosen date, not at a time you choose.
  </Step>
  <Step title="Submit">
    Click **Submit**. The request goes out right away — by email, and also by
    text message if the candidate has a phone number — unless you set a
    scheduled date.

    ![The reference request form: choose a question set, number of references, and managers-only option](/images/reference-request-modal.png)
  </Step>
</Steps>

## The fast path: Quick request

If the candidate isn't in Vitay yet, the **Quick request** button in the left
navbar creates the candidate and sends the reference request in one step —
enter the candidate's details and the request options in a single form.

<Note>
  When you pick a language in the Quick request form, question sets that aren't
  fully translated into that language are hidden from the dropdown, and a
  plain-text hint tells you how many sets were hidden because their translation
  is incomplete.
</Note>

## What happens next

- The candidate gets an email (and a text message, if they have a phone number)
  with a link to enter their references.
- If your firm has turned on candidate document upload, the candidate can
  also attach documents (a résumé, certificates) on the same form — see
  [Collect documents from candidates](/candidates-reference-requests/candidate-documents).
- As each reference is added, Vitay sends that person the question set — by
  email, or by text message when only a phone number was provided.
- Vitay automatically sends reminders if a reference doesn't respond — see
  [Reminders and how they work](/candidates-reference-requests/reminders-and-cadence).
- You can resend, cancel, or fix a recipient at any time — see
  [Manage in-flight requests](/candidates-reference-requests/manage-in-flight-requests).

## Troubleshooting

<AccordionGroup>
  <Accordion title="The candidate says they didn't get the email">
    See [A reference didn't receive the email](/candidates-reference-requests/reference-not-received)
    for the checks to run (correct address, spam, resend).
  </Accordion>
  <Accordion title="Vitay says a request has already been sent to this candidate">
    A candidate can only have **one open request per question set** at a time.
    If a request using that question set is still outstanding, Vitay refuses
    the new one with *"You have already sent a reference request to this
    candidate"*.

    You have two ways forward: pick a **different question set**, which is
    allowed and creates a second, separate request; or deal with the open one
    first — it disappears from the check once the candidate submits it, or once
    you cancel it. See
    [Manage in-flight requests](/candidates-reference-requests/manage-in-flight-requests).
  </Accordion>
  <Accordion title="I can't select a question set">
    If the dropdown is empty, no question set is available to your account. Ask
    a root recruiter at your firm to set one up — see
    [Question sets and templates](/account-configuration/question-sets-and-templates).
    A set is disabled in the dropdown when it has no questions. In the **Quick
    request** form specifically, picking a language also hides sets that aren't
    fully translated into that language — switch language, or complete the
    translations in the question-set editor.
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Add a candidate" icon="user-plus" href="/candidates-reference-requests/add-a-candidate" />
  <Card title="Reminders and how they work" icon="bell" href="/candidates-reference-requests/reminders-and-cadence" />
  <Card title="Manage in-flight requests" icon="paper-plane" href="/candidates-reference-requests/manage-in-flight-requests" />
  <Card title="Read a completed reference" icon="clipboard-check" href="/candidates-reference-requests/read-a-completed-reference" />
  <Card title="Reference request settings" icon="sliders" href="/account-configuration/reference-request-settings" />
</CardGroup>

{/* source: app.vitay.io src/app/shared/components/modals/reference-requests/modals.reference-requests.component.ts (default question set preselect, always-on notes, setting-gated language/scheduled date); modals/quick-request/modals.quick-request.component.ts (translation-filtered question sets + hidden-sets hint); api app/models/reference_request.rb (email + SMS delivery; min_references validated 1..10, nil falls back to setting.min_reference_count; scheduled/for_today scopes); api app/controllers/api/v1/reference_requests_controller.rb#check_reference_request (DuplicateReferenceRequest when candidate.pending_requests already uses that question_set; 'You have already sent a reference request to this candidate'); lib/tasks/reference_requests.rake (reference_requests:scheduled enqueues ReferenceRequest.scheduled.for_today, run daily); app.vitay.io modals/reference-requests component (min_manager_references dropdown = [min_reference_count .. +4], or [1..5] when referenceRequestWasSent; bsConfig.minDate = today when schedule_references is on) */}
