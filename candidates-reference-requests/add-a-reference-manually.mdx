---
title: "Add a reference manually"
description: "Enter a reference yourself instead of sending a request."
---

In most cases, Vitay collects references by asking the candidate to provide their contacts. But sometimes you already have a reference's details — or you need to enter them on the candidate's behalf. Adding a reference manually lets you do that without going through the candidate.

<Note>
  You need a recruiter (or higher) role to add references manually. The
  candidate must already exist in Vitay. See
  [Add a candidate](/candidates-reference-requests/add-a-candidate).
</Note>

## Add a reference manually

<Steps>
  <Step title="Open the candidate">
    Go to **Candidates** and select the candidate you want to add a reference for.
  </Step>
  <Step title="Go to the References tab">
    On the candidate page, click the **References** tab to see all references
    for this candidate.
  </Step>
  <Step title="Add a reference">
    Click **Add reference** (or the equivalent button on the References tab).

    ![SCREENSHOT: the References tab with the "Add reference" button highlighted — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Fill in the reference's details">
    Complete the form with the reference's contact information:

    - **First name** and **Last name** — required.
    - **Email** and/or **Mobile number** — at least one is required. Vitay
      sends the reference request to the email address, or by text message if
      you only enter a phone number. (Some firms turn on a setting that makes
      the email address itself required — it's off by default.)
    - **Company name** — the company where the reference worked with the candidate.
    - **Reference job title** — the reference's job title.
    - **Reference type** — the reference's relationship to the candidate (e.g., Direct Manager, Peer, HR).
    - **Start date** / **End date** — the period when the reference worked with the candidate. These fields only appear when your firm has reference dates enabled.
    - **Question set** — the set of questions this reference will answer.
    - **Language** — the language the request will be sent in (if multi-language is enabled).

    ![SCREENSHOT: the Add reference form with fields filled in — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Save and send">
    Click **Submit** (or **Save**). Vitay creates the reference record and
    sends the reference request right away — by email, or by text message if
    you only provided a phone number.
  </Step>
</Steps>

## What happens next

- The reference receives an email (or a text message, for phone-only
  references) with a link to the question form.
- Vitay automatically sends reminders if they don't respond — see [Reminders and how they work](/candidates-reference-requests/reminders-and-cadence).
- Once they complete the form, you'll see the answers on their reference record. See [Read a completed reference](/candidates-reference-requests/read-a-completed-reference).

## Difference between a manual reference and a reference request

| | Reference request | Manual reference |
|---|---|---|
| Who provides the contact details | The candidate | You (the recruiter) |
| Who gets the initial email | The candidate | The reference directly |
| Useful when | You want the candidate to enter their own contacts | You already have the reference's details |

Both result in the same experience for the reference: they receive an email (or text message) with a link to complete the question form.

## Troubleshooting

<AccordionGroup>
  <Accordion title="I can't see the Add reference button">
    The button is hidden only when your firm requires the candidate's
    permission before references can be added and this candidate hasn't
    granted it yet. Ask the candidate to grant permission (or ask a root
    recruiter about the permission setting), then add the reference.
  </Accordion>
  <Accordion title="The reference says they didn't get the email">
    See [A reference didn't receive the email](/candidates-reference-requests/reference-not-received)
    for a step-by-step troubleshooting guide: confirm the address is correct,
    check for a bounce, and resend if needed.
  </Accordion>
  <Accordion title="Vitay says the email, phone number or ID is already linked to an existing reference">
    Your firm has **Prevent duplicate references** switched on — it is on by
    default, and a root recruiter can turn it off on the **Configuration**
    settings tab. While it is on, Vitay refuses a reference whose **email
    address**, **mobile number** (with the same country code) or **internal
    ID** already belongs to another reference on the *same candidate* for the
    *same question set*.

    So the same person can be used as a reference for a different candidate, or
    for the same candidate under a different question set — but not twice for
    the same candidate and question set. If you meant to update the existing
    reference, open it and edit it instead of adding a new one.
  </Accordion>
  <Accordion title="The email field shows 'personal emails not allowed'">
    Your firm has switched off **Allow personal email addresses**, so Gmail,
    Yahoo, Hotmail, AOL, Outlook, iCloud and similar domains are rejected in
    this modal as well as on the candidate's own form. Ask the candidate for
    the reference's work address, or ask a root recruiter to review
    [Reference request settings](/account-configuration/reference-request-settings).
  </Accordion>
  <Accordion title="I entered the wrong email address">
    Open the reference record, click **Edit**, and update the **Email** field.
    Then use **Send reminder** to resend the request to the corrected address.
    See [Manage in-flight requests](/candidates-reference-requests/manage-in-flight-requests).
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Send a reference request" icon="paper-plane" href="/candidates-reference-requests/send-a-reference-request" />
  <Card title="Manage in-flight requests" icon="sliders" href="/candidates-reference-requests/manage-in-flight-requests" />
  <Card title="Reminders and how they work" icon="bell" href="/candidates-reference-requests/reminders-and-cadence" />
  <Card title="Read a completed reference" icon="clipboard-check" href="/candidates-reference-requests/read-a-completed-reference" />
</CardGroup>

{/* source: api app/models/recruiter_reference.rb (send_reference_email, relationship_choice enum, contact_type_choice enum); app/models/reference.rb (columns: first_name, last_name, email, phone, company_name, title, relationship, start_date, end_date, question_set_id, language); app.vitay.io src/app/shared/services/references.service.ts (addCandidateReference); api app/models/reference.rb#check_uniqueness + exists_by_email?/exists_by_phone?/exists_by_internal_id? + uniqueness_scope (gated on setting.is_prevent_duplicate_references_enabled, default true; scoped to candidate + question_set); db/schema.rb settings.is_prevent_duplicate_references_enabled default true; app.vitay.io projects/configuration settings/configuration SETTINGS.CONFIGURATION.is_prevent_duplicate_references_enabled 'Prevent duplicate references'; src/app/shared/validators/phoneEmailPersonal.validators.ts + personalEmailAllowed.validators.ts (WORK_EMAILS_REGEX, settings_json.is_personal_emails_allowed === false or relationship prevent_personal_emails), src/app/shared/components/form/form-group (FORMS.ERRORS.PERSONAL_EMAIL_ALLOWED '(personal emails not allowed)'); note: the recruiter modal passes only the firm setting, not the per-relationship prevent_personal_emails flag */}
