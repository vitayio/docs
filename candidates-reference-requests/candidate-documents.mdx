---
title: "Collect documents from candidates"
description: "Ask candidates to attach a résumé, certificates, or other documents when they submit their references, and find those files on the candidate's Documents tab."
---

Candidate document upload adds a **Documents** section to the reference request
form your candidates fill in. Candidates can attach files alongside their
references — a résumé, certifications, a licence — so your team has them
before the references come back. You can make the upload optional or required.

<Note>
  You need a **root recruiter** role to turn candidate document upload on or
  off and to edit its settings. Any recruiter with access to the candidate can
  view and download the uploaded files.
</Note>

## Turn on candidate document upload

<Steps>
  <Step title="Open the request settings">
    Go to **Configuration** → **Settings** → **Ref. Requests**.
  </Step>
  <Step title="Enable the card">
    Find the **Candidate document upload** card and switch it on. Changes on
    this card save automatically.

    ![SCREENSHOT: the Ref. Requests settings page with the Candidate document upload card switched on, showing the Required toggle and the description editor — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Decide whether a document is required">
    Switch on **Require at least one document before the candidate can
    submit** if candidates must attach something. When it's off, the
    Documents section is optional and a candidate can submit their references
    without a file.
  </Step>
  <Step title="Write the instructions candidates see">
    Use the **Description shown to candidates above the upload area** editor
    to tell candidates what to attach — for example "Please upload your
    current résumé and any professional certifications." If your account has
    several languages, pick each language in turn and write the text for it;
    candidates see the version matching the language their request was sent
    in.
  </Step>
</Steps>

## What candidates see

When the setting is on, every reference request form shows a **Documents**
card between the references and the signature area, with your description
above it. Candidates drag and drop files onto the card or click **Browse
files**, and can remove a file before submitting.

If you leave the description empty, candidates see Vitay's default text
instead, so the card never appears without an explanation:

> Please upload any documents requested, for example your CV or résumé,
> certificates or licences.

Under the upload area every candidate also sees a fixed privacy notice that
can't be edited. It names your firm as the recipient of the files, says that
your firm decides how they are used and how long they are kept, states where
the files are stored (United States, Canada, or United Kingdom, matching your
firm's Vitay environment), and tells the candidate to contact your firm to get
a copy of their files or ask for them to be deleted. In English it reads:

> The files you upload go to *Your firm*, who decides how they are used and
> how long they are kept. Vitay stores them in *the United States* on *Your
> firm*'s behalf. Contact *Your firm* to request a copy of your files or ask
> for them to be deleted.

The notice deliberately says nothing about **why** the documents are being
collected. That is what your own description above the upload area is for, so
write it to say what you will use the files for.

<Note>
  Because the notice points candidates at your firm for access and deletion
  requests, make sure whoever handles those requests knows that candidate
  documents live on the candidate's **Documents** tab in Vitay.
</Note>

The description and the notice both appear in the language the request was
sent in (English, French, Canadian French, or Spanish).

![SCREENSHOT: the Documents card on the public reference request form showing the default description above the dropzone and the privacy notice beneath it — placeholder](/images/placeholder.png)

Vitay accepts **PDF, DOC, DOCX, PNG, and JPG** files up to **10 MB each**, and
at most **5 files per request**. Files are checked on upload by their actual
content, not just their name — a renamed file of another type is rejected, and
so is a filename containing more than one dot (`resume.pdf.exe`).

If you made the upload required, the section is marked with an asterisk and
the candidate can't submit until at least one file has been uploaded.

Once a request has been submitted, the candidate can no longer add or remove
documents. If they need to send something afterwards, they can email it to you
and you can add it to their record.

## Upload a document yourself

You don't have to wait for the candidate. Any recruiter who can see the
candidate can add a document to their record:

<Steps>
  <Step title="Open the candidate's Documents tab">
    Go to **Candidates**, open the candidate, and click the **Documents** tab.
  </Step>
  <Step title="Add the file">
    Drag the file onto the toolbar at the top right of the tab, or click
    **Select file** and pick it. The upload starts immediately and the list
    refreshes with a **Document uploaded** confirmation.

    ![SCREENSHOT: the candidate Documents tab with the Select file button in the toolbar — placeholder](/images/placeholder.png)
  </Step>
</Steps>

Recruiter uploads accept a wider set of document formats than the candidate
form does: **PDF**, **DOC/DOCX**, **XLS/XLSX**, **PPT/PPTX**, **TXT**, **RTF**,
**ODT/ODS/ODP**, **MD**, **EPUB**, **Pages** and **Keynote**. Images are not
accepted here — a PNG or JPG is rejected as an invalid file type, even though
the file picker may offer them. A filename with more than one dot is rejected
too, and files over 15 MB are refused with **File is too big**.

The tab lists every document on the candidate, whoever added it: **Name**,
**Type**, **Uploaded by**, the date, a status, and buttons to download or
delete. On demo accounts the **Select file** button is hidden.

## Malware scanning

Every document that reaches Vitay — from the candidate's form or from your own
upload — is scanned for malware.

If a file is flagged, it is held back rather than deleted straight away. On the
Documents tab it shows a red **Quarantined** badge, and the download button is
replaced with a dash; clicking the row tells you *"Flagged by malware scanning.
It cannot be downloaded and will be removed automatically."* Nobody on your
team can open the file.

Quarantined files are deleted automatically once they have been in quarantine
for **7 days**, and the removal is recorded in the candidate's activity
history. If you still need the document, ask the candidate to scan their copy and
send you a clean version to upload yourself.

## Find the uploaded documents

- **Candidate page** — open the candidate and click the **Documents** tab. The
  tab shows a count of the candidate's documents, and candidate uploads sit
  alongside any documents your team added. Download or delete them there.
- **Candidate side panel** — the **Documents** tab in the candidate's side
  panel lists the same files with a **Download** button, so you can grab them
  without leaving the list you're on.
- **Completion email** — the "reference request completed" email your team
  receives when the candidate submits links to the candidate's page. It never
  carries the files themselves: emailed copies would sit in mailboxes outside
  Vitay, where they can't be recalled or erased when you delete the candidate
  or their documents.

![SCREENSHOT: the candidate page Documents tab showing a candidate-uploaded résumé with the count badge on the tab — placeholder](/images/placeholder.png)

## Troubleshooting

<AccordionGroup>
  <Accordion title="A candidate says their file is rejected">
    Check the file against the rules above: the type must be PDF, DOC, DOCX,
    PNG, or JPG; the file must be 10 MB or smaller; and the request can hold
    at most 5 files. A file whose extension doesn't match its real type (for
    example a scanned image saved with a `.pdf` extension) is also rejected.
    Ask the candidate to export a fresh copy in one of the supported formats.
  </Accordion>
  <Accordion title="The Documents card isn't on the request form">
    The setting is off, or it was turned on after the candidate opened the
    form. Check **Settings → Ref. Requests**, then ask the candidate to reload
    the form. A request that has already been submitted never shows the card.
  </Accordion>
  <Accordion title="A document shows a Quarantined badge">
    Malware scanning flagged the file. It cannot be downloaded by anyone, and
    Vitay deletes it automatically after 7 days in quarantine. Ask the
    candidate for a clean copy — see [Malware scanning](#malware-scanning)
    above.
  </Accordion>
  <Accordion title="My own upload was rejected as an invalid file type">
    Recruiter uploads accept documents (PDF, Word, Excel, PowerPoint, text,
    RTF, OpenDocument, Markdown, EPUB, Pages, Keynote) but **not images** — a
    PNG or JPG screenshot has to be converted to PDF first. Filenames with more
    than one dot are rejected as well, and the file must be under 15 MB.
  </Accordion>
  <Accordion title="The completion email doesn't include the documents">
    That's by design. The email links to the candidate's page and never
    attaches the files, so that copies don't end up in mailboxes outside
    Vitay's access controls and deletion rules. Open the candidate's
    **Documents** tab to download them.
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Send a reference request" icon="paper-plane" href="/candidates-reference-requests/send-a-reference-request" />
  <Card title="Add a candidate" icon="user-plus" href="/candidates-reference-requests/add-a-candidate" />
  <Card title="Read a completed reference" icon="clipboard-check" href="/candidates-reference-requests/read-a-completed-reference" />
  <Card title="Notification settings" icon="bell" href="/account-configuration/notification-settings" />
</CardGroup>

{/* source: app.vitay.io projects/configuration settings/requests/settings.requests.component (SETTINGS.candidate_documents card: enabled, required, per-language description); projects/references public/reference-requests/reference-requests.component (REFERENCE_REQUESTS.DOCUMENTS card, ngx-file-drop, LIMITS copy); protected/candidates/show/documents + sidebar/candidate/documents (Documents tab count, Download); api app/controllers/api/v1/reference_requests_controller.rb public documents create/destroy (Marcel magic-byte sniffing, 10 MB, 5 files, 422 when required and missing); app/controllers/api/v3/candidates_controller.rb#create_document + ALLOWED_CONTENT_TYPES (recruiter uploads: pdf/doc/docx/xls/xlsx/ppt/pptx/txt/rtf/odt/ods/odp/md/epub/pages/key, no images; multi-extension rejected); app.vitay.io projects/references protected/candidates/show/candidates.show.component.{ts,html} (ngx-file-drop on the Documents tab, BUTTONS.DROP_FILE 'Select file', 15 MB client guard 'File is too big', browse hidden on demo firms), protected/candidates/show/documents/candidates.show.documents.component.ts (Name/Content type/Uploaded by/Created at/Status/Download/Delete columns, CANDIDATE_DOCUMENTS.QUARANTINED + QUARANTINED_HINT, download blocked when quarantined); api app/models/document_malware_scan.rb (GuardDutyMalwareScanStatus tag, THREATS_FOUND), app/services/quarantined_document_purge_service.rb (DEFAULT_GRACE_PERIOD 7.days, delete_document activity source malware_scan); ReferenceRequest#send_reference_request_completed_to_recruiter (links to the candidate page; never attaches documents) */}
