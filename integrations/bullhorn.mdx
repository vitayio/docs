---
title: "Bullhorn integration"
description: "Connect Vitay to Bullhorn so you can push candidates and completed reference reports into Bullhorn and run reference requests without leaving Bullhorn."
---

The Bullhorn integration links Vitay to your Bullhorn account using Bullhorn's OAuth flow. Once connected, your team can push candidates to Bullhorn, upload finished reference PDFs to the candidate's Bullhorn file, and run the whole reference-request flow inside Bullhorn.

<Note>
  Only a **root recruiter** can connect or disconnect the Bullhorn integration. You will need your firm's Bullhorn **Client ID**, **Client Secret**, **username**, and **password** before you start.
</Note>

## What the integration does

- **Push candidates to Bullhorn** — create a Bullhorn candidate record from a Vitay candidate in one click.
- **Push reference reports** — once references are complete, upload a PDF of the reference report directly to the candidate's file in Bullhorn.
- **Sync ownership** — when pushing a candidate to Bullhorn, Vitay matches the recruiter's email to the corresponding Bullhorn corporate user and sets them as the record owner.
- **Work inside Bullhorn** — firms on the in-app experience can request references and read results from tabs, cards, and actions embedded in Bullhorn itself (see below).

## Connect Bullhorn

A root recruiter starts the connection in Vitay:

<Steps>
  <Step title="Open the Bullhorn integration">
    In Vitay, open **Integrations** (a top-level item in the navigation, alongside Settings) and select the **Bullhorn** card.
  </Step>
  <Step title="Enter your Bullhorn API credentials">
    Fill in your Bullhorn **Client ID**, **Client Secret**, **username**, and **password**, then request the authorisation code.
  </Step>
  <Step title="Wait for validation">
    The Vitay team validates the connection before it becomes active. You may need to accept Bullhorn's Terms of Service during this step before your recruiters can use the integration.
  </Step>
</Steps>

If you have questions during setup, contact [hello@vitay.io](mailto:hello@vitay.io).

![SCREENSHOT: Integrations gallery showing the Bullhorn integration dialog with the credential fields — placeholder](/images/placeholder.png)

## Use the integration

Once connected, your recruiters will see a **Synchronize** dropdown on candidate and reference pages:

- **Synchronize to Bullhorn** (on the candidate page header) — creates the candidate record in Bullhorn. A candidate that has already been synced can't be pushed again; you'll see a "Candidate already synched" error instead of a duplicate record.
- **Upload document to Bullhorn** (from **Synchronize** on a completed reference) — generates a PDF of the completed reference and attaches it to the candidate in Bullhorn.

## Work inside Bullhorn

Firms on the Bullhorn in-app experience can run the whole reference-request
flow without leaving Bullhorn. Vitay supports four Bullhorn placements, each
added from Bullhorn's **View Layout** editor using a URL that Vitay generates
for your firm.

<Note>
  The in-app experience is enabled per firm by the Vitay team. If you don't see
  the URL rows described below on the Bullhorn integration in Vitay, contact
  [hello@vitay.io](mailto:hello@vitay.io) to have it turned on.
</Note>

| Placement | What your recruiters get |
| --- | --- |
| **Custom Tab** | A full Vitay tab on the candidate record — request references and read results in place. |
| **Card Tab** | A compact reference-status card on the candidate overview. |
| **Custom Menu Action** | A one-click "Request references" entry in the candidate action menu that opens straight to the request form. |
| **Custom List Action** | Select several candidates in a Bullhorn list and request references for all of them from a single form. |

### Add a placement

<Steps>
  <Step title="Copy the URL from Vitay">
    In Vitay, go to **Integrations** and open the Bullhorn
    integration's configuration. Each placement has its own row with a copy
    button — **Copy embed URL**, **Copy card URL**, **Copy menu action URL**, and
    **Copy list action URL**.

    ![SCREENSHOT: the Bullhorn integration configuration modal showing the four URL copy rows — placeholder](/images/placeholder.png)
  </Step>
  <Step title="Paste it into Bullhorn's View Layout editor">
    In Bullhorn, open the View Layout editor for the Candidate entity and add
    the URL to the matching section: **Custom Tabs**, **Card Tabs**, **Custom
    Menu Actions**, or **Custom List Actions**. Each Vitay row names the
    Bullhorn section it belongs in.
  </Step>
  <Step title="Save and reload Bullhorn">
    Save the layout and reload Bullhorn. The new tab, card, menu entry, or list
    action appears for the users covered by that layout.
  </Step>
</Steps>

<Warning>
  Add these placements only after your Vitay in-app experience has been enabled
  and confirmed. A placement pointing at an integration that isn't active will
  show an error to your recruiters rather than the Vitay form.
</Warning>

### Request references for several candidates at once

<Steps>
  <Step title="Select candidates in Bullhorn">
    Open any Bullhorn candidate list and tick the candidates you want to request
    references for.
  </Step>
  <Step title="Run the Vitay list action">
    Choose the Vitay entry from the list's action menu. A Vitay window opens
    showing how many candidates you selected.
  </Step>
  <Step title="Fill in the request once">
    Complete the request form as you normally would for a single candidate. The
    details you enter apply to every selected candidate.
  </Step>
  <Step title="Submit and check the results">
    Vitay submits one request per candidate and shows a result line for each:
    **requested**, **already in progress** (that candidate had an open request,
    so nothing was duplicated), or **failed**. A tally appears when it finishes.
  </Step>
</Steps>

<Note>
  Leave the window open until the results finish. If you close it partway
  through, the remaining candidates are not requested — just run the list action
  again on those candidates. Re-running it is safe: candidates that already have
  an open request come back as "already in progress" rather than getting a
  second request.
</Note>

## Disconnect Bullhorn

Only a root recruiter can disconnect the integration. Open **Integrations**, select the Bullhorn card, and choose **Reset credentials** in the integration dialog to remove the connection.

## Troubleshooting

<AccordionGroup>
  <Accordion title="I see an authorization error after connecting">
    Bullhorn access tokens expire. Vitay refreshes them automatically, but if the connection breaks (for example, your Bullhorn password changes), the integration will need to be reconnected. Contact [hello@vitay.io](mailto:hello@vitay.io).
  </Accordion>
  <Accordion title="The 'Synchronize to Bullhorn' option is missing">
    The **Synchronize** dropdown is only visible once the Bullhorn integration is connected for your firm. Ask a root recruiter to confirm the integration is active.
  </Accordion>
  <Accordion title="I get a 'Candidate already synched' error when pushing a candidate">
    Pushing a candidate creates the Bullhorn record, and each candidate can only be pushed once. This error means the candidate is already linked to a Bullhorn record — there's nothing more to do.
  </Accordion>
  <Accordion title="A candidate came back as 'already in progress' in a bulk request">
    That candidate already had an open reference request in Vitay. Vitay does
    not create a second request while one is in flight — open the candidate in
    Vitay to see the existing request's status.
  </Accordion>
  <Accordion title="Some candidates in a bulk request were never requested">
    The Vitay window was most likely closed before the run finished. Re-run the
    list action on the same candidates; the ones already requested come back as
    "already in progress" and nothing is duplicated.
  </Accordion>
  <Accordion title="A reference report won't upload to Bullhorn">
    The candidate must first be synced to Bullhorn (via **Synchronize to Bullhorn**) before a document can be attached. If the candidate already exists in Bullhorn but the sync record is missing, contact [hello@vitay.io](mailto:hello@vitay.io).
  </Accordion>
</AccordionGroup>

## Related articles

<CardGroup cols={2}>
  <Card title="Integrations overview" icon="plug" href="/integrations/overview" />
  <Card title="JobAdder integration" icon="briefcase" href="/integrations/jobadder" />
  <Card title="Add a candidate" icon="user-plus" href="/candidates-reference-requests/add-a-candidate" />
  <Card title="Send a reference request" icon="paper-plane" href="/candidates-reference-requests/send-a-reference-request" />
</CardGroup>

{/* source: api app/controllers/api/bullhorn_controller.rb; api app/services/external_services/bullhorn/authorize.rb; api app/models/external_authorisations/bullhorn.rb; api embed/list-action controllers (embed_request, status, list_action); app.vitay.io projects/references embed surfaces (tab/card/list), projects/configuration MODALS.INTEGRATIONS.{EMBED,CARD,MENU,LIST}_URL */}
